interface:
  display_name: "项目共享记忆 V02"
  short_description: "在项目文件夹中部署轻量共享记忆，保留历史并检查多人回写冲突"
  default_prompt: "使用 $project-shared-memory 为当前项目初始化或升级 V02 本地共享记忆，保留已有记录。"

policy:
  allow_implicit_invocation: true
